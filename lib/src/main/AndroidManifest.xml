<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.mbkj.lib" >

    <application>
        <activity android:launchMode="singleTask" android:name=".LibAcitivity" android:screenOrientation="portrait" android:theme="@style/libSplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="design_width_in_dp"
            android:value="375"/>
        <meta-data
            android:name="design_height_in_dp"
            android:value="695"/>
    </application>
</manifest>
